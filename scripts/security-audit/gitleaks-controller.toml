# This policy is loaded only from the protected controller checkout. Audited
# commits cannot replace it with repository-local configuration or allowlists.
title = "SPE security audit controller policy"
minVersion = "8.30.1"

[extend]
useDefault = true
